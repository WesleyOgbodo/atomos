import { Link } from 'react-router-dom'
import { useAuth } from '@/contexts/AuthContext'

export function Account() {
  const { user, profile, logout } = useAuth()

  return <main className="mx-auto max-w-5xl px-5 py-10"><div className="flex flex-col justify-between gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Account</p><h1 className="mt-2 text-3xl font-black tracking-tight">{profile?.full_name || user?.email}</h1><p className="mt-1 text-sm text-slate-500">{user?.email}</p></div><button onClick={() => void logout()} className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Sign out</button></div><div className="mt-8 grid gap-5 sm:grid-cols-2"><section className="rounded-2xl border border-slate-200 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Account type</p><p className="mt-2 text-lg font-bold capitalize">{profile?.role ?? 'buyer'}</p><p className="mt-1 text-sm text-slate-500">{profile?.role === 'seller' ? 'You can publish and manage listings from the seller area.' : 'Seller access will be handled through the Atomos seller onboarding flow.'}</p></section><section className="rounded-2xl border border-slate-200 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Account status</p><p className="mt-2 text-lg font-bold">{profile?.is_suspended ? 'Suspended' : 'Active'}</p><p className="mt-1 text-sm text-slate-500">Your authentication session is managed by Supabase.</p></section></div><div className="mt-6 flex flex-wrap gap-3"><Link to="/wishlist" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Wishlist</Link><Link to="/messages" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Messages</Link><Link to="/cart" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Cart</Link><Link to="/orders" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Orders</Link>{profile?.role === 'seller' && (<><Link to="/account/listings" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">My listings</Link><Link to="/seller/orders" className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Seller orders</Link><Link to="/sell" className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-blue-700">Create listing</Link></>)}</div></main>
}
